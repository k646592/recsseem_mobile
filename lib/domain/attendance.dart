class Attendance {
  Attendance(this.id, this.username, this.title, this.start, this.end, this.description, this.mailSend, this.userId);
  String id;
  String username;
  String title;
  DateTime start;
  DateTime end;
  String description;
  bool mailSend;
  String userId;
}

